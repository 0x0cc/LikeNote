<!--pages/index/index.wxml-->
<!-- 页面标题 -->
<view class="theme_full_title" style="padding-top: {{statusHeight+8}}px;">
  主页
</view>
<!-- 发言 -->
<view bindtap="navWrite" class="fix_button" style="height: {{fix_button}}px; top: {{statusHeight+80}}px;">
  <image src="/icon/add.png" mode="widthFix"></image>
</view>

<!-- 内容 -->
<block wx:for="{{content}}" wx:key="item">
  <view class="full_width_box">
    <!-- 用户信息 -->
    <view class="info_flexbox">
      <view class="avatar_box" style="height: {{avatarHeight}}px;">
        <image src="{{item.avatar}}" mode="widthFix" class="avatar"></image>
      </view>
      <view class="info_box">
        <view class="nickName">{{item.nickName}}</view>
      </view>
    </view>
    <!-- 正文 -->
    <view class="ctn_box">
      <block wx:for="{{item.ctn}}" wx:for-item="ctn" wx:key="ctn">
        <view class="ctn_prg">{{ctn}}</view>
      </block>
    </view>
  </view>
</block>
